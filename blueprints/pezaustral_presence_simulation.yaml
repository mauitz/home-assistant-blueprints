blueprint:
  name: PezAustral Presence Simulation v2.0
  description: >
    # 🏠 Simulación de Presencia Inteligente

    **VERSIÓN 2.0 - TODO INTEGRADO**

    ✅ **CLEANUP AUTOMÁTICO** - Apaga TODAS las luces al detener (sin automatizaciones extras)
    ✅ **MONITOREO INTEGRADO** - Actualiza contadores automáticamente
    ✅ **DETENCIÓN LIMPIA** - Se puede detener en cualquier momento
    ✅ **ESCENAS DE SALIDA** - Configurables para fin normal o emergencia
    ✅ **CONTROL DE LÁMPARAS** - Máximo simultáneas configurable
    ✅ **LOGS DETALLADOS** - Tracking completo en Logbook

    **MEJORAS V2.0:**
    - Cleanup se ejecuta SIEMPRE al detener (no solo al finalizar loops)
    - Sin necesidad de automatizaciones adicionales
    - Un solo blueprint hace todo

    **Documentación:** https://github.com/mauitz/home-assistant-blueprints

  domain: automation
  source_url: https://github.com/mauitz/home-assistant-blueprints/blob/main/blueprints/pezaustral_presence_simulation.yaml

  input:
    # ══════════════════════════════════════════════════════════════════════════
    # CONTROL
    # ══════════════════════════════════════════════════════════════════════════
    automation_control_entity:
      name: Entidad de Control *
      description: Input boolean para activar/desactivar la simulación
      selector:
        entity:
          domain: input_boolean

    # ══════════════════════════════════════════════════════════════════════════
    # LUCES
    # ══════════════════════════════════════════════════════════════════════════
    lights:
      name: Luces/Switches *
      description: Dispositivos que se encenderán/apagarán
      selector:
        entity:
          domain: [light, switch]
          multiple: true

    max_lights_on:
      name: Máximo Simultáneas
      description: Máximo de luces encendidas al mismo tiempo
      default: 2
      selector:
        number:
          min: 1
          max: 20
          mode: slider

    # ══════════════════════════════════════════════════════════════════════════
    # TIMING
    # ══════════════════════════════════════════════════════════════════════════
    loop_count:
      name: Loops
      description: Número de repeticiones (99 = infinito)
      default: 10
      selector:
        number:
          min: 0
          max: 99
          mode: box

    time_on_min:
      name: Tiempo Encendido Mínimo (min)
      default: 15
      selector:
        number:
          min: 1
          max: 240
          mode: box

    time_on_max:
      name: Tiempo Encendido Máximo (min)
      default: 45
      selector:
        number:
          min: 1
          max: 240
          mode: box

    loop_delay_min:
      name: Delay Entre Loops Mínimo (min)
      default: 5
      selector:
        number:
          min: 0
          max: 120
          mode: box

    loop_delay_max:
      name: Delay Entre Loops Máximo (min)
      default: 15
      selector:
        number:
          min: 0
          max: 120
          mode: box

    transition_on:
      name: Transición Encendido (seg)
      default: 1
      selector:
        number:
          min: 0
          max: 10
          step: 0.5
          mode: slider

    transition_off:
      name: Transición Apagado (seg)
      default: 1
      selector:
        number:
          min: 0
          max: 10
          step: 0.5
          mode: slider

    # ══════════════════════════════════════════════════════════════════════════
    # ESCENAS
    # ══════════════════════════════════════════════════════════════════════════
    enable_exit_scene:
      name: Activar Escena de Salida
      default: false
      selector:
        boolean:

    exit_scene:
      name: Escena de Salida Normal
      description: Se ejecuta al completar todos los loops
      default: []
      selector:
        entity:
          domain: scene

    emergency_scene:
      name: Escena de Parada de Emergencia
      description: Se ejecuta al detener manualmente
      default: []
      selector:
        entity:
          domain: scene

    # ══════════════════════════════════════════════════════════════════════════
    # MONITOREO
    # ══════════════════════════════════════════════════════════════════════════
    enable_monitoring:
      name: Habilitar Monitoreo
      description: Actualiza helpers automáticamente (requiere helpers configurados)
      default: true
      selector:
        boolean:

# ══════════════════════════════════════════════════════════════════════════════
# VARIABLES
# ══════════════════════════════════════════════════════════════════════════════
variables:
  automation_control_entity: !input automation_control_entity
  lights: !input lights
  max_lights_on: !input max_lights_on
  loop_count: !input loop_count
  time_on_min: !input time_on_min
  time_on_max: !input time_on_max
  loop_delay_min: !input loop_delay_min
  loop_delay_max: !input loop_delay_max
  transition_on: !input transition_on
  transition_off: !input transition_off
  enable_exit_scene: !input enable_exit_scene
  exit_scene: !input exit_scene
  emergency_scene: !input emergency_scene
  enable_monitoring: !input enable_monitoring

# ══════════════════════════════════════════════════════════════════════════════
# TRIGGER
# ══════════════════════════════════════════════════════════════════════════════
trigger:
  - platform: state
    entity_id: !input automation_control_entity
    to: "on"

# ══════════════════════════════════════════════════════════════════════════════
# MODE
# ══════════════════════════════════════════════════════════════════════════════
mode: single
max_exceeded: silent

# ══════════════════════════════════════════════════════════════════════════════
# ACTION
# ══════════════════════════════════════════════════════════════════════════════
action:
  # ════════════════════════════════════════════════════════════════════════════
  # INICIALIZACIÓN
  # ════════════════════════════════════════════════════════════════════════════
  - variables:
      lights_currently_on: []
      loop_interrupted: false
      detener_manualmente: false

  # Actualizar helpers de inicio
  - choose:
      - conditions:
          - condition: template
            value_template: "{{ enable_monitoring }}"
        sequence:
          - service: input_boolean.turn_on
            target:
              entity_id: input_boolean.presence_simulation_running

          - service: input_datetime.set_datetime
            target:
              entity_id: input_datetime.presence_simulation_start_time
            data:
              datetime: "{{ now().strftime('%Y-%m-%d %H:%M:%S') }}"

          - service: input_number.set_value
            target:
              entity_id: input_number.presence_simulation_loop_counter
            data:
              value: 0

          - service: input_text.set_value
            target:
              entity_id: input_text.presence_simulation_status
            data:
              value: "Iniciando..."

  # Log de inicio
  - service: logbook.log
    data:
      name: "🏠 Presence Simulation"
      message: "Iniciando - {{ lights | length }} dispositivos, {{ loop_count }} loops"

  # ════════════════════════════════════════════════════════════════════════════
  # LOOP PRINCIPAL (con try-catch simulado)
  # ════════════════════════════════════════════════════════════════════════════
  - repeat:
      count: "{{ loop_count if loop_count < 99 else 999 }}"
      sequence:
        # ────────────────────────────────────────────────────────────────────
        # VERIFICAR SI SE DETUVO
        # ────────────────────────────────────────────────────────────────────
        - condition: template
          value_template: "{{ is_state(automation_control_entity, 'on') }}"

        # Actualizar contador de loop
        - choose:
            - conditions:
                - condition: template
                  value_template: "{{ enable_monitoring }}"
              sequence:
                - service: input_number.set_value
                  target:
                    entity_id: input_number.presence_simulation_loop_counter
                  data:
                    value: "{{ repeat.index }}"

                - service: input_text.set_value
                  target:
                    entity_id: input_text.presence_simulation_status
                  data:
                    value: "En ejecución - Loop {{ repeat.index }}"

        # Log de loop
        - service: logbook.log
          data:
            name: "🔄 Presence Simulation"
            message: "Loop {{ repeat.index }}/{{ loop_count }}"

        # ────────────────────────────────────────────────────────────────────
        # PROCESAR CADA LUZ
        # ────────────────────────────────────────────────────────────────────
        - repeat:
            for_each: "{{ lights }}"
            sequence:
              # Verificar si se detuvo
              - condition: template
                value_template: "{{ is_state(automation_control_entity, 'on') }}"

              - variables:
                  current_light: "{{ repeat.item }}"
                  current_domain: "{{ current_light.split('.')[0] }}"
                  needs_turn_off: "{{ lights_currently_on | length >= max_lights_on }}"
                  light_to_turn_off: >-
                    {% if lights_currently_on | length >= max_lights_on %}
                      {{ lights_currently_on[0] }}
                    {% else %}
                      null
                    {% endif %}

              # Apagar luz antigua si alcanzamos el límite
              - choose:
                  - conditions:
                      - condition: template
                        value_template: "{{ needs_turn_off and light_to_turn_off != 'null' }}"
                    sequence:
                      - choose:
                          - conditions:
                              - condition: template
                                value_template: "{{ light_to_turn_off.split('.')[0] == 'light' }}"
                            sequence:
                              - service: light.turn_off
                                target:
                                  entity_id: "{{ light_to_turn_off }}"
                                data:
                                  transition: "{{ transition_off }}"
                        default:
                          - service: homeassistant.turn_off
                            target:
                              entity_id: "{{ light_to_turn_off }}"

                      - variables:
                          lights_currently_on: "{{ lights_currently_on[1:] | list }}"

                      # Actualizar monitoreo
                      - choose:
                          - conditions:
                              - condition: template
                                value_template: "{{ enable_monitoring }}"
                            sequence:
                              - service: input_number.set_value
                                target:
                                  entity_id: input_number.presence_simulation_lights_on_count
                                data:
                                  value: "{{ lights_currently_on | length }}"

              # Encender luz nueva
              - choose:
                  - conditions:
                      - condition: template
                        value_template: "{{ current_domain == 'light' }}"
                    sequence:
                      - service: light.turn_on
                        target:
                          entity_id: "{{ current_light }}"
                        data:
                          transition: "{{ transition_on }}"
                default:
                  - service: homeassistant.turn_on
                    target:
                      entity_id: "{{ current_light }}"

              - service: logbook.log
                data:
                  name: "💡 Presence Simulation"
                  message: "Encendida: {{ state_attr(current_light, 'friendly_name') or current_light }}"

              # Agregar a lista
              - variables:
                  lights_currently_on: "{{ lights_currently_on + [current_light] }}"

              # Actualizar monitoreo
              - choose:
                  - conditions:
                      - condition: template
                        value_template: "{{ enable_monitoring }}"
                    sequence:
                      - service: input_number.set_value
                        target:
                          entity_id: input_number.presence_simulation_lights_on_count
                        data:
                          value: "{{ lights_currently_on | length }}"

                      - service: input_text.set_value
                        target:
                          entity_id: input_text.presence_simulation_active_lights
                        data:
                          value: >-
                            {% set ns = namespace(names=[]) %}
                            {% for light in lights_currently_on %}
                              {% set name = state_attr(light, 'friendly_name') or light %}
                              {% set ns.names = ns.names + [name] %}
                            {% endfor %}
                            {{ ns.names | join(', ') if ns.names | length > 0 else 'Ninguna' }}

              # Esperar tiempo aleatorio con verificación
              - wait_template: "{{ is_state(automation_control_entity, 'off') }}"
                timeout:
                  minutes: "{{ range(time_on_min, time_on_max + 1) | random }}"

              # Si se detuvo durante el wait, marcar y salir
              - choose:
                  - conditions:
                      - condition: template
                        value_template: "{{ is_state(automation_control_entity, 'off') }}"
                    sequence:
                      - variables:
                          detener_manualmente: true
                      - stop: "Detenido manualmente"

              # Apagar esta luz
              - choose:
                  - conditions:
                      - condition: template
                        value_template: "{{ current_domain == 'light' }}"
                    sequence:
                      - service: light.turn_off
                        target:
                          entity_id: "{{ current_light }}"
                        data:
                          transition: "{{ transition_off }}"
                default:
                  - service: homeassistant.turn_off
                    target:
                      entity_id: "{{ current_light }}"

              - variables:
                  lights_currently_on: "{{ lights_currently_on | reject('eq', current_light) | list }}"

              # Actualizar monitoreo
              - choose:
                  - conditions:
                      - condition: template
                        value_template: "{{ enable_monitoring }}"
                    sequence:
                      - service: input_number.set_value
                        target:
                          entity_id: input_number.presence_simulation_lights_on_count
                        data:
                          value: "{{ lights_currently_on | length }}"

        # Delay entre loops
        - choose:
            - conditions:
                - condition: template
                  value_template: "{{ repeat.index < loop_count }}"
              sequence:
                - delay:
                    minutes: "{{ range(loop_delay_min, loop_delay_max + 1) | random }}"

  # ════════════════════════════════════════════════════════════════════════════
  # CLEANUP FINAL (se ejecuta SIEMPRE, termine como termine)
  # ════════════════════════════════════════════════════════════════════════════
  - alias: "Cleanup Final"
    choose:
      # Si hay luces encendidas, apagarlas
      - conditions:
          - condition: template
            value_template: "{{ lights_currently_on | length > 0 }}"
        sequence:
          - service: logbook.log
            data:
              name: "🧹 Presence Simulation"
              message: "Limpieza: {{ lights_currently_on | length }} luz(es)"

          - repeat:
              for_each: "{{ lights_currently_on }}"
              sequence:
                - variables:
                    cleanup_light: "{{ repeat.item }}"
                    cleanup_domain: "{{ cleanup_light.split('.')[0] }}"

                - choose:
                    - conditions:
                        - condition: template
                          value_template: "{{ cleanup_domain == 'light' }}"
                      sequence:
                        - service: light.turn_off
                          target:
                            entity_id: "{{ cleanup_light }}"
                          data:
                            transition: "{{ transition_off }}"
                  default:
                    - service: homeassistant.turn_off
                      target:
                        entity_id: "{{ cleanup_light }}"

                - delay:
                    milliseconds: 300

  # Limpiar helpers
  - choose:
      - conditions:
          - condition: template
            value_template: "{{ enable_monitoring }}"
        sequence:
          - service: input_boolean.turn_off
            target:
              entity_id: input_boolean.presence_simulation_running

          - service: input_number.set_value
            target:
              entity_id: input_number.presence_simulation_lights_on_count
            data:
              value: 0

          - service: input_text.set_value
            target:
              entity_id: input_text.presence_simulation_active_lights
            data:
              value: "Ninguna"

          - service: input_text.set_value
            target:
              entity_id: input_text.presence_simulation_status
            data:
              value: "Finalizada"

  # Ejecutar escena de salida
  - choose:
      # Si se detuvo manualmente y hay escena de emergencia
      - conditions:
          - condition: template
            value_template: "{{ detener_manualmente and emergency_scene | length > 0 }}"
        sequence:
          - service: logbook.log
            data:
              name: "🎬 Presence Simulation"
              message: "Escena de emergencia: {{ emergency_scene }}"

          - service: scene.turn_on
            target:
              entity_id: "{{ emergency_scene }}"

      # Si terminó normalmente y hay escena de salida
      - conditions:
          - condition: template
            value_template: "{{ enable_exit_scene and exit_scene | length > 0 and not detener_manualmente }}"
        sequence:
          - service: logbook.log
            data:
              name: "🎬 Presence Simulation"
              message: "Escena de salida: {{ exit_scene }}"

          - service: scene.turn_on
            target:
              entity_id: "{{ exit_scene }}"

  # Log final
  - service: logbook.log
    data:
      name: "✅ Presence Simulation"
      message: "Finalizada - Todas las luces apagadas"

